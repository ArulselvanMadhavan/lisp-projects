;; -*- lexical-binding: t -*-
;; Pascal triangle
;; (require 'seq)
;; (seq-reduce (lambda(acc x)(+ acc x)) '(1 2 3 4) 0)

;; (defun helper (n prev-row))

;; (defun pascal-triangle (n))
(defun pascal-triangle (n)
  )